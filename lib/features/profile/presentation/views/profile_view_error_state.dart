import 'package:flutter/material.dart';

class ProfileViewErrorState extends StatelessWidget {
  const ProfileViewErrorState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
